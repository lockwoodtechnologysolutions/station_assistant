# Route ALSA through the HA host PulseAudio socket (provided by audio: true).
# PyAudio uses ALSA, which this config bridges to PulseAudio, which gives
# access to the USB audio device passed through by HA Supervisor.
pcm.!default {
    type pulse
}
ctl.!default {
    type pulse
}
